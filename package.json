{
  "name": "cosha",
  "version": "0.2.0",
  "description": "Colorful shadows for your images",
  "entry": "src/cosha.js",
  "main": "dist/cosha.module.js",
  "unpkg": "dist/cosha.browser.js",
  "author": "Robin Löffel",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.11.1",
    "rollup": "^1.21.2",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-copy": "^3.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-terser": "^5.1.2"
  },
  "scripts": {
    "dist": "yarn del && rollup -c --environment prod && surge page",
    "dev": "yarn del && rollup -c -w & http-server page -g -s -o",
    "prod": "yarn del && rollup -c -w --environment prod & http-server page -g -s -o",
    "del": "rm -f {page,dist}/cosha.{browser,module}.*",
    "pblsh": "yarn dist && yarn publish"
  },
  "files": [
    "dist"
  ]
}
